<Window x:Class="TerrainGenerationTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TerrainGenerationTool"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Image x:Name="HeightmapImage" HorizontalAlignment="Left" Height="100" Margin="10,10,0,0" VerticalAlignment="Top" Width="100" Source="{Binding FilePath}"/>
        <TextBlock HorizontalAlignment="Left" Margin="131,21,0,0" TextWrapping="Wrap" Text="Heightmap" VerticalAlignment="Top"/>
        <TextBox x:Name="PathTextBox" HorizontalAlignment="Left" Margin="131,51,0,0" TextWrapping="Wrap" Text="{Binding FilePath}" VerticalAlignment="Top" Width="300"/>
        <Button x:Name="PathButton" Content="Select" HorizontalAlignment="Left" Margin="436,49,0,0" VerticalAlignment="Top" Command="{Binding SelectCommand}"/>
        <Button x:Name="SaveButton" Content="Save" HorizontalAlignment="Left" Margin="436,86,0,0" VerticalAlignment="Top" Command="{Binding SaveCommand}"/>
        <TextBox x:Name="SizeXTextBox" HorizontalAlignment="Left" Margin="131,109,0,0" TextWrapping="Wrap" Text="{Binding XValue}" VerticalAlignment="Top" Width="50" />
        <TextBox x:Name="SizeYTextBox" HorizontalAlignment="Left" Margin="191,109,0,0" TextWrapping="Wrap" Text="{Binding YValue}" VerticalAlignment="Top" Width="50"/>
        <TextBox x:Name="MagnitudeTextBox" HorizontalAlignment="Left" Margin="131,149,0,0" TextWrapping="Wrap" Text="{Binding MagnitudeValue}" VerticalAlignment="Top" Width="100"/>
        <TextBlock x:Name="StatusText" HorizontalAlignment="Left" Margin="131,199,0,0" TextWrapping="Wrap" Text="{Binding StatusValue}" VerticalAlignment="Top" Width="100"/>
    </Grid>
</Window>
